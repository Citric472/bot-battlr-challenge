// src/components/BotSpecs.js
import React from 'react';

const BotSpecs = ({ bot, onEnlist }) => {
  return (
    <div>
      <h2>{bot.name} Specs</h2>
      {/* Display bot details */}
      {/* Include buttons to go back and enlist */}
    </div>
  );
};

export default BotSpecs;
